# Every version pinned with ==. Determinism (see docs/adr/0003) is a property of
# the whole environment, not of the seed; a floated minor version silently breaks it.
torch==2.5.1
numpy==2.1.3
pynacl==1.5.0
psycopg2-binary==2.9.10
fastapi==0.115.6
uvicorn[standard]==0.34.0
xgboost==2.1.3
